@AGENTS.md

## Journal des évolutions

Après chaque évolution mobile, ajouter ici une entrée de 2 phrases (quoi + où), la plus récente en haut, pour garder le suivi d'une conversation à l'autre.

- **Issue #14 — CRUD playlists (local)** (2026-07-06) : gestion complète des playlists locales (créer/renommer/supprimer, ajouter/retirer/réordonner, lecture, persistance). **Persistance** : **migration SQLite v4** dans `db.ts`, cette fois **purement additive** (tables `playlists` + `playlist_tracks`, contrairement à v2/v3 on ne touche **pas** à `tracks`, donc aucun re-scan) ; `playlist_tracks` référence une piste par son id de media store (`tracks.id`) avec `position` (ordre) et clé (playlist, piste) qui interdit les doublons ; fonctions CRUD (`createPlaylist`/`renamePlaylist`/`deletePlaylist`/`addTracksToPlaylist`/`removeTrackFromPlaylist`/`setPlaylistTrackOrder`/`loadPlaylists`/`loadPlaylistTrackIds`, le réordonnancement **réécrit toutes les positions** plutôt que de bricoler des index). **État** : `usePlaylists.ts` (liste + `revision` incrémenté à chaque mutation ; les ids de pistes d'une playlist sont **relus en base** via `getTrackIds`, mémoïsés sur `revision` — SQLite reste l'unique source de vérité, pas de duplication d'état) exposé par un `PlaylistsProvider` (même motif que `LibraryProvider`/`PlayerProvider`, monté dans `_layout.tsx`). `useAudioLibrary` expose en plus `tracksById` (**toutes** les pistes scannées, dossiers exclus compris) pour résoudre les références d'une playlist en `LocalTrack` : **choix produit** = une piste ajoutée reste jouable même si son dossier est décoché ensuite ; une piste dont le fichier a disparu est juste omise de l'affichage/lecture, sa référence reste en base sans casser l'ordre des autres. **UI** : 4e segment **Playlists** dans l'onglet biblio (`app/(tabs)/index.tsx`) — le `SegmentedControl` devient **défilable horizontalement** pour tenir 4 labels sans clipper sur écran étroit (liseré bas gardé pleine largeur) ; écran détail poussé `app/playlist.tsx` (header nom/compteur, bouton Lire, renommer/supprimer, liste réordonnable) ; `PlaylistNameDialog` (modal `TextInput` create+rename, car `Alert.prompt` est iOS-only) ; `PlaylistPickerSheet` (feuille « Ajouter à une playlist » ouverte depuis le long-press via une nouvelle action de `TrackActionsSheet`, avec entrée « Nouvelle playlist »). **Réutilisation** : `DraggableQueueList` généralisé en `DraggableTrackList` (item normalisé `{id,title,artist,artworkUri}` + `removeLabel`), consommé par la file **et** le détail playlist (la file projette `Track` RNTP, la playlist projette `LocalTrack`). Icônes `delete`/`edit`/`playlist_add_check`/`check` ajoutées. ⚠️ **Limite assumée** : le segment Playlists n'apparaît que si la biblio est prête et non vide (`hasContent`) — une playlist ne référence que des pistes locales, donc sans musique elle est vide de sens ; à revoir si on veut gérer des playlists hors ligne. ⚠️ Ids de playlist = UUIDv4 pseudo-aléatoire local (`Math.random`, pas d'usage crypto, pas de dep native ajoutée) ; la synchro backend (references artiste/titre/MBID, cf. PROJET.md) remplacera/complétera la clé `track_id` sans changer le schéma. Non testé sur appareil (pas d'émulateur). Lint/format/typecheck/export web OK.
- **Suivi #13 — Numéro de disque (TPOS) + tri multi-disques** (2026-07-03) : le tri d'album se faisait sur `trackNo` seul, ce qui entremêlait les disques d'un album multi-disques. `id3.ts` parse maintenant **TPOS/TPA** (numéro de disque, helper `parsePosition` mutualisé avec TRCK) propagé jusqu'en base : nouvelle colonne `disc_no` (**migration SQLite v3**, même parti pris qu'en v2 = `DELETE FROM tracks` pour forcer la relecture du tag, dossiers/exclusions préservés) via `trackTags`→`useAudioLibrary`→`db`. Dans `grouping.ts`, l'ordre d'album passe par `byDiscThenTrack` (disque, puis piste, puis titre ; valeurs manquantes repoussées en fin, donc mono-disque inchangé) et un nouveau `groupAlbumByDisc` découpe en sections. `app/album.tsx` devient une `SectionList` : en-tête « Disque N » affiché **seulement** si l'album a plusieurs disques (index de piste per-section, file de lecture à plat via `indexById`). **Choix produit rappelé** : pour des fichiers possédés, le tag local (TRCK/TPOS) reste la source d'ordre ; MusicBrainz a bien la position mais elle est *release-dépendante* (matcher LA bonne release = plus coûteux que artiste/album/pochette), donc réservé à un futur fallback. Non testé sur appareil (pas d'émulateur). Lint/format/typecheck/export web OK.
- **Issues #11 (liste morceaux) + #12 (artistes) + #13 (albums)** (2026-07-03) : vue Bibliothèque à trois sous-vues via un `SegmentedControl` maison (`src/components/SegmentedControl.tsx`, style Forge) en tête de `app/(tabs)/index.tsx` : **Morceaux** (liste virtualisée + `SortBar` bascule titre/artiste), **Artistes** (liste agrégée → détail), **Albums** (grille 2 colonnes de pochettes → détail). Deux écrans détail poussés sur la stack racine (comme `library-settings`) : `app/artist.tsx` (SectionList, une section par album, en-tête d'album cliquable vers le détail album ; file de lecture = toutes les pistes de l'artiste ordonnées album→n° piste) et `app/album.tsx` (pistes **dans l'ordre** n° de piste puis titre, gros header pochette). Composants mutualisés extraits : `TrackRow` (lit désormais les tags **directement** depuis `LocalTrack`), `TrackCover` (variante `fill` pour les vignettes d'album), `BackButton` ; logique de regroupement pure et partagée dans `src/library/grouping.ts` (`buildArtists`/`buildAlbums`/`tracksForArtist`/`tracksForAlbum`/`makeAlbumKey`/`sortTracks`, clé d'album = album artist + séparateur + titre pour distinguer les homonymes). **Point clé d'archi (la vraie décision)** : artiste/album n'étaient lus qu'à la volée par ligne (`useTrackTags`, jamais persistés) — impossible de regrouper/trier sur toute la biblio. On lit donc maintenant les tags **au scan** et on les **persiste en base** : nouvelles colonnes `artist`/`album`/`album_artist`/`track_no`/`artwork_uri` (**migration SQLite v2** dans `db.ts`, qui `DELETE FROM tracks` pour forcer **un** re-scan complet — dossiers/exclusions préservés) ; `extractTrackTags(assetId, uri)` (sync, ajouté à `trackTags.ts`) est appelé dans `resolveRows` avec l'URI déjà résolue par `getUri` (pas de second aller-retour) et peuple base **+** cache mémoire ; `id3.ts` parse en plus TPE2 (album artist) et TRCK (n° piste). `useTrackTags.ts` supprimé (devenu inutile). Icônes `sort`/`person`/`album` ajoutées. Non testé sur appareil (pas d'émulateur). ⚠️ Même piège de types de routes que #22 : `/artist` et `/album` font râler `tsc` tant que metro n'a pas régénéré `.expo/types` (CI verte via fallback `Href=string`). Lint/format/typecheck/export web OK.
- **Issue #22 — Exclusion de dossiers / fichiers du scan (réglages)** (2026-07-03) : nouvel écran `app/library-settings.tsx` (accessible depuis la ligne « Bibliothèque locale » de `app/(tabs)/settings.tsx`, désormais cliquable avec hint dynamique `n/total dossiers · x exclus`) listant les dossiers détectés avec un `Switch` par dossier (inclure/scanner) + une section « Fichiers exclus » réinclusibles ; exclusion fichier par fichier via une action « Exclure de la bibliothèque » ajoutée au `TrackActionsSheet` (long-press). **Blocklist** : tout inclus par défaut, auto-exclusion des sonneries/notifs/alarmes + `Android/media|data/*` (`isAutoExcluded` dans `folders.ts`, appliqué au premier passage via `ensureFolderPref`, choix ensuite mémorisé en base). **Point clé d'archi** : `useAudioLibrary` porte un état SQLite en mémoire, donc l'écran biblio et l'écran réglages doivent partager *la même* instance — sinon un toggle ne se répercuterait pas ; extraction d'un `LibraryProvider` (contexte `useLibrary`, monté dans `_layout.tsx` au-dessus de `PlayerProvider`, même motif que le lecteur), l'onglet biblio et les réglages consomment ce contexte. Ajout des icônes `folder`/`block`/`add`. ⚠️ Les types de routes expo-router (`.expo/types`, gitignoré) sont régénérés par `expo start`/metro, pas par `expo export` : un `tsc` local peut râler sur une route neuve tant que metro n'a pas tourné (la CI part d'un checkout frais sans le fichier → fallback `Href=string`, donc verte). Non testé sur appareil (pas d'émulateur). Lint/format/typecheck/export web OK.
- **Issue #10 — Persistance locale de la bibliothèque (SQLite)** (2026-07-03) : ajout de `expo-sqlite` et d'une couche `src/library/db.ts` (tables `tracks`/`folder_prefs`/`excluded_tracks`, `PRAGMA user_version` pour la migration, API synchrone `getAllSync`/`runSync`/`withTransactionSync`, tout gardé par `Platform.OS !== 'web'`). `useAudioLibrary.ts` refondu : l'état est **initialisé synchroniquement depuis SQLite** (`useState(() => db.loadTracks())` → cache affiché sans flash au démarrage, plus de re-scan complet bloquant) puis un **scan incrémental** en tâche de fond fait un *diff par id* contre la base — seuls les **nouveaux** fichiers passent par `Asset.getUri()` (pool de concurrence 8) pour résoudre leur **dossier** (`folderOf`, car le media store « Next » n'expose pas le chemin), les disparus sont supprimés. Nouveau `refreshing` (scan de fond par-dessus le cache) affiché dans le sous-titre biblio ; les champs `folders`/`excludedTracks`/`setFolderIncluded`/`setTrackExcluded` préparent l'issue exclusion (commit suivant). `src/library/folders.ts` (pur) : `folderOf`/`displayFolder`/`isAutoExcluded`. ⚠️ Comme RNTP, l'impl **web** d'expo-sqlite casse l'export (`import .wasm`) : même parade que shaka (web-shim `src/library/sqlite.web-shim.js` + alias dans `metro.config.js`). Non testé sur appareil (pas d'émulateur). Lint/format/typecheck/export web OK.
- **Issues #8 (background/notif/écran verrouillé) + #9 (écran Now Playing)** (2026-07-03) : **#8 était déjà couverte** par le moteur (issue #6) — lecture app tuée (`AppKilledPlaybackBehavior.ContinuePlayback` + `registerPlaybackService` au niveau module dans `index.js`), contrôles notif (`notificationCapabilities` Play/Pause/Next/Prev dans `setup.ts` relayés par `service.ts` via `Event.Remote*`), écran verrouillé (même MediaSession Android), métadonnées titre/artiste/pochette (`track.ts` remplit `title/artist/artwork`) ; côté natif, le manifest de `react-native-track-player` déclare déjà `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_MEDIA_PLAYBACK` et le service `foregroundServiceType="mediaPlayback"` (mergé au prebuild), donc **aucun ajout de permission requis**. **#9 : seul écart réel** au niveau « vraie app musicale » = le seek du now-playing était **tap-only** alors que le knob promet un glissement ; ajout du **drag-to-seek** (`app/now-playing.tsx`) via `PanResponder` du cœur RN (comme la file, reanimated/gesture-handler non configurés) : `scrubFraction` locale pendant le geste (le knob + les labels de temps suivent le doigt, knob agrandi `progressKnobActive`), `seekTo` appelé **au lâcher seulement** pour ne pas bombarder le lecteur ; largeur/durée/callback lus via une ref mise à jour hors rendu (effet) pour ne pas recréer le responder en plein geste (motif + `eslint-disable react-hooks/refs` identiques à `DraggableQueueList`). ⚠️ Notif/écran verrouillé **non testés sur appareil** (pas d'émulateur ici) ; sur Android 13+, la visibilité de la notif dépend de `POST_NOTIFICATIONS` accordée (à valider sur device). Lint/format/typecheck/export web OK.
- **Issue #7 — File d'attente + gestion de l'ordre** (2026-07-03) : `PlayerProvider` gère un snapshot réactif de la file (nouveau contexte `useQueue` : `{ tracks, activeIndex }`, resynchronisé sur `Event.PlaybackActiveTrackChanged` + après chaque mutation) et expose `addToQueue`/`playNext`/`removeFromQueue`/`moveInQueue`/`skipToIndex` (API queue de RNTP : `add`/`remove`/`move`/`getQueue`/`getActiveTrackIndex`) ; `resolvePlayerTracks` extrait de `track.ts` factorise `LocalTrack[]→Track[]`. **Toutes les mutations de file sont sérialisées** (`enqueue`, chaîne de promesses via ref) + `refreshQueue` a un garde-fou anti-résultat périmé (numéro de séquence) : sans ça, des déplacements rapides entrelaçaient les lectures natives non atomiques `getQueue`+`getActiveTrackIndex` et le snapshot désignait la mauvaise piste active. Côté UI : écran modal `app/queue.tsx` (tap→saut, croix→retirer) ouvert depuis l'icône `queue_music` du now-playing, et menu long-press `TrackActionsSheet` sur les lignes de la bibliothèque (« Lire ensuite » / « Ajouter à la file »). **Réordonnancement par glisser-déposer maison** (`DraggableQueueList`, `Animated` + `PanResponder` du cœur RN, poignée `drag_indicator` dédiée pour ne pas capter le tap ni le scroll ; rendu à positions absolues sur grille `ROW_HEIGHT=64`, ordre optimiste synchronisé via le motif « ajuster l'état pendant le rendu ») : **choix assumé de ne PAS prendre de lib DnD** (react-native-reorderable-list) car reanimated 4/gesture-handler sont dans les deps mais **non configurés** (pas de `babel.config.js`/plugin worklets, pas de `GestureHandlerRootView`) et imposeraient un rebuild natif risqué sur RN 0.85/new arch. Surlignage de la piste active **par id** (`activeTrackId`), pas par index, pour rester juste pendant un réordonnancement. Stub web RNTP complété (`add`/`remove`/`move`/`getQueue`/`getActiveTrackIndex`) ; lint/format/typecheck/export web OK. ⚠️ **DnD non testé sur appareil** (pas d'émulateur ici) : auto-scroll en bord d'écran non géré (une file tient surtout à l'écran) ; à valider sur device. Lecture séquentielle déjà acquise (`RepeatMode.Queue`, issue #6).
- **Issue #6 — Moteur de lecture (react-native-track-player)** (2026-07-03) : intégration de `react-native-track-player` **5.0.0-alpha0** avec un module `src/player/` (service headless `service.ts` pour les commandes distantes, `setup.ts` init idempotente + capacités, `track.ts` LocalTrack→Track résolvant URI (`Asset.getUri()`) + tags/pochette, `PlayerProvider.tsx` exposant les actions play/toggle/next/prev/seek et `usePlayback.ts` l'état réactif via les hooks RNTP). Entry point custom `index.js` (enregistre le service, garde web), UI câblée (tap piste => file + lecture dans `app/(tabs)/index.tsx`, `MiniPlayer` et `now-playing` sur l'état réel + seek au tap) ; web étant hors périmètre (Android only), RNTP tirant `shaka-player`, on stub RNTP sur le web (`metro.config.js` + `src/player/trackPlayer.web-shim.js`) et on passe `web.output` à `single` (le prerender statique était incompatible). **Choix de version (important)** : la stack impose la New Architecture (Reanimated 4), or RNTP **4.1.2 (MIT)** est ancienne archi et crashe au runtime (TurboModule interop : « returnType == void iff synchronous ») ; la v5 **stable** est republiée sous `@rntp/player` mais en **licence propriétaire** (gratuite usage perso, payante en pro). On reste donc sur `react-native-track-player@5.0.0-alpha0`, dernière version **libre (MIT)** compatible new arch (vrai TurboModule, `NativeTrackPlayerSpec`). ⚠️ **Choix assumé mais fragile** (release unique, non maintenue) : si l'alpha casse un jour (bump Expo/RN, bug de lecture), ne pas s'acharner à la patcher, migrer vers `@rntp/player` 5.6.0+ (stable, gratuit usage perso mais proprio) ou `expo-audio` (libre/stable, first-party ; gère écran verrouillé + notif via `setActiveForLockScreen` / `enableBackgroundPlayback` dès le SDK 56, mais seulement play/pause + seek — pas de boutons piste suivante/précédente sur la notif, contrairement à RNTP qui est file-native). ⚠️ Build natif **sous JDK 21** obligatoire : jenv peut laisser `JAVA_HOME` sur un JDK plus récent qui casse la config CMake/prefab (« restricted method in java.lang.System ») ; forcer `JAVA_HOME` sur openjdk@21 pour un `gradlew` direct (`expo run:android` en shell interactif prend déjà 21). Lint/format/typecheck/export web OK.
- **Issue #5 — Tags ID3 + pochettes** (2026-07-02) : parser ID3 maison en pur JS (`src/library/id3.ts`, ID3v2.2/2.3/2.4 + APIC + repli ID3v1, encodages latin1/UTF-8/UTF-16) alimenté par une couche IO (`src/library/trackTags.ts`) qui fait une lecture *partielle* du fichier via `expo-file-system` (`FileHandle`, `Asset.getUri()` → `file://`) et écrit les pochettes en cache disque. Lecture paresseuse par ligne (`src/library/useTrackTags.ts`) branchée sur `TrackRow` (`app/(tabs)/index.tsx`) qui affiche pochette + artiste/album avec repli propre (nom de fichier, pastille braise) ; ajout de `expo-file-system` aux deps, lint/format/typecheck/export web OK.
- **Issue #4 — Permission audio + scan bibliothèque locale** (2026-07-02) : ajout de `expo-media-library` (plugin `app.json` limité à `READ_MEDIA_AUDIO` via `granularPermissions: ['audio']`) et d'un hook `src/library/useAudioLibrary.ts` qui demande la permission puis scanne l'audio du media store (`Query.exeForMetadata`, API « Next »). L'écran Bibliothèque (`app/(tabs)/index.tsx`) gère tous les états (loading/scanning/undetermined/denied/vide/liste) et affiche les titres détectés ; lint, format, typecheck et export web OK.
- **Issue #3 — Qualité mobile (Prettier + CI)** (2026-07-02) : ajout de Prettier (`.prettierrc.json`, `.prettierignore`, `eslint-config-prettier` dans `eslint.config.js`, scripts `format`/`format:check`) et d'un workflow `.github/workflows/ci.yml` (job quality lint+format+typecheck, job build Android via `expo prebuild` + `gradlew assembleDebug` sous JDK 21). Ajout d'un `exclude` dans `tsconfig.json` (example/android/ios/dist).
- **Issue #2 — Navigation + thème de base** (2026-07-02) : mis en place le thème Direction B « Forge » (tokens dans `src/theme/`, icônes Material Symbols par ligature via `src/components/Icon.tsx`, police Schibsted Grotesk) et la navigation (Tabs Bibliothèque/Réglages via `expo-router/js-tabs` + tab bar custom `ForgeTabBar`, écran Lecture en modal). Trois écrans placeholder créés (`app/(tabs)/index.tsx`, `app/(tabs)/settings.tsx`, `app/now-playing.tsx`) ; typecheck, lint et export web OK.
